version: 2
updates:
  - package-ecosystem: "gradle" # Gradle/Kotlin dependencies (build.gradle.kts, buildSrc, build-logic)
    directory: "/"
    schedule:
      interval: "weekly"

  - package-ecosystem: "github-actions" # Workflow action versions in .github/workflows
    directory: "/"
    schedule:
      interval: "weekly"
